# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build todo-list-be`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t todo-list-be`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system todo-list-be && \
          adduser --system -G todo-list-be todo-list-be

COPY dist/apps/todo-list-be todo-list-be/
RUN chown -R todo-list-be:todo-list-be .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix todo-list-be --omit=dev -f install

CMD [ "node", "todo-list-be" ]
